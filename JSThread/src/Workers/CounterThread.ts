import Thread, { initThread } from "./WorkerThread";

class CounterWorker extends Thread {
    // Empty for now. Thread is quite extensible so this should be a small class compared to it.
}

initThread()